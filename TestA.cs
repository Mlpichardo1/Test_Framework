using NUnit.Framework;
using OpenQA.Selenium;
using OpenQA.Selenium.Firefox;

namespace Test_Framework
{
    [TestFixture]
    [Ignore("skip this test")]
    public class TestA
    {
        [Test]
        public void TestMethod()
        {
            // ExtentReports extent = new ExtentReports(@"C:\Users\User\report.html");
            // var test = extent.StartTest("Test A", "Sample Description");
            // test.log(LogStatus.Info, "Validating the download link")
            



            // IWebDriver driver = new FirefoxDriver();
            // driver.Url = "http://www.seleniumhq.org/";
            // driver.Manage().Window.Maximize();

            // IWebElement linkText = driver.FindElement(By.LinkText("Download"));
            // linkText.Click();

            // extent.EndTest(test);
            // extent.Flush();
            // driver.Quit();
        }
    }
}